<%= form_for [current_user,@tweet], remote: true do |f| %>
  <div class="form-group">


    <%= f.label :text %>
    <%= f.text_area :message, class: 'form-control', :html => {:row => "8"} %>
  </div>
  
  <button type="submit" class="btn btn-default">Submit</button>  
<% end %>